<div id="content">
  <% for mode in @modes %>

    <div id="<%= mode.title %>" class="droppable ui-widget-header">
      <p>message</p>
        <% for note in mode.notes  %>
      
          <div id="<%= note.id %>" class="draggable ui-widget-content"> 
            <%= note.title %> : <%= note.description %> 
          </div>
       
        <% end %>
      </div>

  <% end %>
</div>

